import * as React from 'react';
import Calendar from '../container/Carendar';
import Modal from '../container/Modal';

export default () => (
    <div>
        <Calendar />
        <Modal />
    </div>
);
